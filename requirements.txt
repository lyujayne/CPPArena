# CPP Arena 依赖（Python 3.8+，推荐 3.10+）
# GUI
PySide6>=6.5
# 几何计算 / 坐标转换
shapely>=2.0
pyproj>=3.0
# 数值与表格
numpy>=1.24
pandas>=1.5
# 可视化（支持 PNG 300dpi / SVG / PDF 矢量导出）
matplotlib>=3.7
# KML 解析
lxml>=4.9
